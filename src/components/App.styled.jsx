import styled from '@emotion/styled';

export const Container = styled.div`
  margin: 0 auto;
  margin-top: 20px;
  padding: 20px;
  max-width: 580px;

  box-shadow: var(--main-shadow);
  border-radius: 10px;
`;
